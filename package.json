{
  "version": "1.0.0",
  "license": "ISC",
  "workspaces": [
    "dephium-app",
    "dephium-contracts"
  ],
  "scripts": {
    "dev": "npm run dev -w dephium-app",
    "build:app": "npm run build -w dephium-app",
    "build:contracts": "npm run build -w dephium-contracts",
    "compile": "npm run compile -w dephium-contracts",
    "deploy": "npm run deploy -w dephium-contracts",
    "clean": "npm run clean -w dephium-contracts"
  }
}
