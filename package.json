{
  "name": "dephium",
  "license": "ISC",
  "version": "1.0.0",
  "workspaces": [
    "app",
    "contracts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=dephium-app",
    "compile": "npm run compile --workspace=dephium-contracts",
    "deploy": "npm run deploy --workspace=dephium-contracts"
  }
}
