{
  "version": "1.0.0",
  "license": "ISC",
  "workspaces": [
    "dephium-app",
    "dephium-contracts"
  ],
  "scripts": {
    "dev": "npm run dev -w dephium-app",
    "build": "npm run build -w dephium-app",
    "compile": "npm run compile -w dephium-contracts",
    "deploy": "npm run deploy -w dephium-contracts"
  }
}
